{
  "name": "grid-test",
  "version": "1.0.0",
  "description": "",
  "main": "src/Index.js",
  "scripts": {
    "clean": "rimraf build",
    "prettier": "prettier --write src/**/*.ts",
    "tslint": "tslint --fix --project ./tsconfig.json 'src/**/*.{ts,tsx}'",
    "build": "tsc",
    "start": "ts-node src/Index.ts",
    "pre-commit": "npm run clean && npm run prettier && npm run tslint && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ts-node": "^8.4.1",
    "typescript": "^3.7.2",
    "winston": "^3.2.1"
  },
  "devDependencies": {
    "@types/node": "^12.12.7",
    "prettier": "^1.19.0",
    "rimraf": "^3.0.0",
    "tslint": "^5.20.1",
    "tslint-sonarts": "^1.9.0"
  }
}
